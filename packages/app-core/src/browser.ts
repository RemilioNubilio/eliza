/**
 * Browser-safe entry point for renderer bundles.
 *
 * Today the package root is still the compatibility surface consumed across the
 * repo, so keep the browser entry aligned with it to avoid split-brain exports.
 */

export * from "./index";
